# Optional production webhook verification
RAZORPAY_WEBHOOK_SECRET=replace_with_a_32_plus_character_secret
ALLOW_UNSIGNED_DEMO_WEBHOOKS=true

# Optional: where the demo webhook receiver stores events
WEBHOOK_EVENT_FILE=data/webhook_events.jsonl
